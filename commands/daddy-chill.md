---
description: Turn Daddy Chill on, turn it off, or show its current status.
---

Use the Daddy Chill mode command requested by the user: `$ARGUMENTS`.

Valid modes are `on`, `off`, and `status`. Report the new state briefly. Do not add extra style instructions because the plugin hook controls the active context.
